
- Экран зафиксирован (нет скролла).
- Любая `btn-<имя>` открывает `/public/svg/<имя>.svg`.
- Крестик/назад закрывают модалку; если их нет — тап по фону.
- Если файла нет — показывается тост «Файл не привязан: <имя>.svg», **ничего не открываем**.
